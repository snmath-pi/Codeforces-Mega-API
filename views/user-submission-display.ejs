<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Codeforces Submissions</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        height: 100vh;
        background-color: #f4f4f4;
        display: flex;
        justify-content: center;
        align-items: flex-start;
      }
      .container {
        text-align: center;
        max-width: 800px;
        width: 100%;
        padding: 20px;
        background-color: #fff;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        border-radius: 5px;
        margin-top: 20px;
        overflow-y: auto;
        max-height: 80vh;
      }
      h1 {
        font-size: 24px;
        margin-bottom: 20px;
        color: #333;
      }
      .submission-info {
        border: 1px solid #ddd;
        padding: 15px;
        border-radius: 5px;
        background-color: #fafafa;
        margin: 10px 0;
        text-align: left;
      }
      .submission-info h3 {
        margin: 0 0 10px 0;
        font-size: 18px;
        color: #444;
      }
      .submission-info .field {
        margin-bottom: 10px;
      }

      /* Modern Blue Scrollbar */
      ::-webkit-scrollbar {
        width: 12px;
      }
      ::-webkit-scrollbar-track {
        background: #e0f7fa;
        border-radius: 6px;
      }
      ::-webkit-scrollbar-thumb {
        background-color: #0288d1;
        border-radius: 6px;
        border: 2px solid #e0f7fa;
      }
      ::-webkit-scrollbar-thumb:hover {
        background-color: #0277bd;
      }
      /* For Firefox */
      body {
        scrollbar-width: thin;
        scrollbar-color: #0288d1 #e0f7fa;
      }

      /* Link Styling */
      .link-container {
        margin-top: 20px;
      }
      .link-container a {
        display: inline-block;
        padding: 10px 20px;
        margin: 5px;
        font-size: 16px;
        color: #0288d1;
        text-decoration: none;
        background-color: #f0f0f0;
        border-radius: 5px;
        transition: background-color 0.3s, color 0.3s;
      }
      .link-container a:hover {
        background-color: #0288d1;
        color: #fff;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Last <%= count %> Submissions</h1>
      <div id="result">
        <% if (submissions && submissions.length > 0) { %> <%
        submissions.forEach(function(submission) { %>
        <div class="submission-info">
          <h3>
            Problem: <%= submission.problem.name %> (Contest ID: <%=
            submission.contestId %>, Submission ID: <%= submission.id %>)
          </h3>
          <div class="field">
            <strong>Verdict:</strong> <%= submission.verdict || "N/A" %>
          </div>
          <div class="field">
            <strong>Language:</strong> <%= submission.programmingLanguage %>
          </div>
          <div class="field">
            <strong>Time Consumed:</strong> <%= submission.timeConsumedMillis %>
            ms
          </div>
          <div class="field">
            <strong>Memory Consumed:</strong> <%= submission.memoryConsumedBytes
            %> bytes
          </div>
        </div>
        <% }) %> <% } else { %>
        <h2>No submissions found or an error occurred.</h2>
        <% } %>
      </div>
      <div class="link-container">
        <a href="/user-submissions">Back to User Submissions Page</a>
        <a href="/">Back to Home Page</a>
      </div>
    </div>
  </body>
</html>
